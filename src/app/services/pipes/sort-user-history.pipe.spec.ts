import { SortUserHistoryPipe } from './sort-user-history.pipe';

describe('SortUserHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortUserHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
